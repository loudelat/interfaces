using System;
namespace interfaces
{
    public class Square
    {
		double length;

        // overloaded constructor
		public Square(double length)
        {
			this.length = length;
        }
    }
}
